spring.application.name=StuEventManagement_Application

# Server configuration
server.port=${PORT:8080}

# MongoDB configuration (Ensure you set MONGO_URL in Railway environment variables)
spring.data.mongodb.uri=${MONGO_URL:mongodb://localhost:27017/test}
